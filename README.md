# Client-Side Tampering Detection

This project provides a simple, fully client-side JavaScript solution for detecting web page tampering. Tampering detection ensures critical assets or scripts have not been modified by the user or automated tools.

## Features
- Verifies critical scripts’ integrity.
- Detects DOM manipulations.
- Alerts user or logs if tampering is detected.

## How to Use

1. Include `tampering.js` in your web project.
2. Configure target elements, scripts, or hashes as needed.
3. See `example.html` for demonstration.

## Example

